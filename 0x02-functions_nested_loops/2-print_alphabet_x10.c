#include "main.h"

/**
 * print_alphabet -  print alphabet in lowercase
 */

void print_alphabet(void)
{
	int count = 1;

	while (i >= 10)
	{
	for (char ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);

	_putchar('\n');
	}
}

