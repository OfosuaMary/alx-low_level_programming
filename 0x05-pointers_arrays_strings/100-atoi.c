#include "main.h"
/**
 * _atoi - converts string to integer
 * @s: pointer parameter
 * Return: Integer
 */
int _atoi(char *s)
{
	int i = 0;

	while (*(s + i) != '\0')
	{
		if(*(s + i) >= 0
		i++;
	}
}
